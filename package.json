{
    "scripts": {
        "build": "dfx build",
        "deploy": "dfx deps deploy && dfx deploy && dfx canister deposit-cycles 35000000000000 stadiumFactory && dfx canister deposit-cycles 35000000000000 teamFactory",
        "reset": "dfx stop && dfx start --clean --background && npm run deploy"
    }
}