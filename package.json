{
    "scripts": {
        "build": "dfx build",
        "deploy": "dfx deploy",
        "reset": "dfx stop && dfx start --clean --background && dfx deps deploy && dfx deploy --with-cycles 30000000000000"
    }
}