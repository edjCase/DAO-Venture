{
    "scripts": {
        "build": "dfx build",
        "deploy": "dfx deploy",
        "reset": "dfx stop && dfx start --clean --background && dfx deps deploy && dfx deploy && dfx canister deposit-cycles 35000000000000 stadiumFactory && dfx canister deposit-cycles 35000000000000 teamFactory"
    }
}